package grupo3.LabFingeso.repository;

import org.springframework.data.jpa.repository.JpaRepository;

public interface tipoUsuarioPermisoRepository extends JpaRepository<tipoUsuarioPermisoRepository, Long> {
}
